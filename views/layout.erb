<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Text Analyzer</title>
    <link rel="stylesheet" href="<%= "#{url 'stylesheets/analyzer.css'}" %>">
  </head>
  <body>
    <% if session[:message] %>
      <p class="message"><%= session.delete(:message) %></p>
    <% end %>
    <%= yield %>
  </body>
</html>
