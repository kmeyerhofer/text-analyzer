<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Text Analyzer</title>
  <link rel="stylesheet" href="<%= url 'stylesheets/analyzer.css' %>">
</head>
<body>
  <% if session[:flash_message] %>
    <p class="flash-message"><%= session.delete(:flash_message) %></p>
  <% end %>
  <%= yield %>
</body>
</html>
